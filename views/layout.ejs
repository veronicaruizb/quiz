<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Preguntas</title>
    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon" />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' type='text/css' media='only screen and (min-width: 800px) and (min-device-width: 600px)' href='/stylesheets/wide.css' />
    <link rel='stylesheet' type='text/css' media='only screen and (max-width: 480px)' href='/stylesheets/smartphone.css' />
  </head>
  <body>
  <div id="page">
    <header class="main">
    <h2>Quiz<span class="no-narrow">: el juego de las preguntas</span></h2>
    </header>

    <nav class="main" role="navigation">
      <a href="/">Inicio</a>
      <a href="/quizzes">Preguntas</a>
      <a href="/author">Creditos</a>
    </nav>

    <section id="f">
      <% if(messages.info){ %>
          <article if="flashinfo">
            <h3>Información de interés: </h3>
            <ul>
              <% for(var i in messages.info){ %>
              <li> <%= messages.info[i] %> </li>
              <% } %>
            </ul>
          </article>
      <% } %>
      <% if(messages.sucess){ %>
          <article if="flashsuccess">
            <h3>Tareas completadas con éxito: </h3>
            <ul>
              <% for(var i in messages.sucess){ %>
              <li> <%= messages.sucess[i] %> </li>
              <% } %>
            </ul>
          </article>
      <% } %>
      <% if(messages.error){ %>
          <article if="flasherror">
            <h3>Errores encontrados: </h3>
            <ul>
              <% for(var i in messages.error){ %>
              <li> <%= messages.error[i] %> </li>
              <% } %>
            </ul>
          </article>
      <% } %>
    </section>

    <section class="main">
    <div><%- body%></div>
    </section>

    <footer class="main">
      <a href="http://es.wikipedia.org/wiki/Quiz">¿Qué es un Quiz?</a>
      <a href="https://github.com/veronicaruizb/quiz.git">Enlace a GitHub</a>
    </footer>
  </body>
</html>
